export default interface IUser {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
  }

// File ini berisi interface TypeScript yang mendefinisikan
// tipe data user, misalnya IUser. Interface ini digunakan
// untuk memaksa struktur tertentu pada data user yang diterima
// dari API atau digunakan dalam aplikasi. 